# Local dev: point the CMS at Zenith on Saya over Tailscale.
# In production the CMS is served same-origin with the API, so this is left unset
# and requests go to /api/cms/... relative to the CMS hostname.
PUBLIC_ZENITH_URL=http://saya:7181
