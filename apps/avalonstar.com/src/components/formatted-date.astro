---
interface Props {
  date: Date
}

const { date } = Astro.props
---

<time datetime={date.toISOString()} class="font-caps tracking-wide">
  {
    date.toLocaleDateString('en-us', {
      month: 'long',
      day: 'numeric',
      year: 'numeric'
    })
  }
</time>
